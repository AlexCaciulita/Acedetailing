# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Runtime data stays out, but the finish-record seed is source and must ship —
# note this has to ignore the directory CONTENTS, since git cannot re-include a
# file whose parent directory is itself excluded.
data/*
!data/records.json

# Editor directories and files
.claude/settings.local.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify
